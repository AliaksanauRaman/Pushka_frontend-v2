import { TranslatedPlaceViewPipe } from './translated-place-view.pipe';

describe('TranslatedPlaceViewPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatedPlaceViewPipe();
    expect(pipe).toBeTruthy();
  });
});
