import { ApplicationDirective } from './application.directive';

describe('ApplicationDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplicationDirective();
    expect(directive).toBeTruthy();
  });
});
